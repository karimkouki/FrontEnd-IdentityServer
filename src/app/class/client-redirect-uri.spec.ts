import { ClientRedirectUri } from './client-redirect-uri';

describe('ClientRedirectUri', () => {
  it('should create an instance', () => {
    expect(new ClientRedirectUri()).toBeTruthy();
  });
});
